"use strict";(self.webpackChunkmoobius_doc=self.webpackChunkmoobius_doc||[]).push([[357],{6599:(e,n,s)=>{s.r(n),s.d(n,{assets:()=>l,contentTitle:()=>o,default:()=>m,frontMatter:()=>i,metadata:()=>c,toc:()=>d});var t=s(4848),a=s(8453);const i={id:"CanvasButton",label:"Canvas Button",title:"Canvas, Button and Context Menu"},o=void 0,c={id:"tutorial-basics/CanvasButton",title:"Canvas, Button and Context Menu",description:"Canvas",source:"@site/versioned_docs/version-1.0.0/tutorial-basics/Canvas Button.md",sourceDirName:"tutorial-basics",slug:"/tutorial-basics/CanvasButton",permalink:"/moobius/1.0.0/tutorial-basics/CanvasButton",draft:!1,unlisted:!1,editUrl:"https://github.com/groupultra/moobius/edit/main/versioned_docs/version-1.0.0/tutorial-basics/Canvas Button.md",tags:[],version:"1.0.0",lastUpdatedBy:"hangyi",lastUpdatedAt:1725873532e3,frontMatter:{id:"CanvasButton",label:"Canvas Button",title:"Canvas, Button and Context Menu"},sidebar:"docs",previous:{title:"Messages",permalink:"/moobius/1.0.0/tutorial-basics/Messages"},next:{title:"Quick Reference",permalink:"/moobius/1.0.0/tutorial-basics/Quick_Reference"}},l={},d=[{value:"Canvas",id:"canvas",level:2},{value:"Button and Dialog",id:"button-and-dialog",level:2},{value:"Context Menu",id:"context-menu",level:2}];function r(e){const n={admonition:"admonition",code:"code",h2:"h2",img:"img",li:"li",ol:"ol",p:"p",pre:"pre",...(0,a.R)(),...e.components};return(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(n.h2,{id:"canvas",children:"Canvas"}),"\n",(0,t.jsx)(n.p,{children:"Although Message is arguably the most commonly used means of interaction for a Group Based Application, Moobius supports a few other modalities that could substantially enrich the Users' experience."}),"\n",(0,t.jsx)(n.p,{children:"It is easy to display some text and/or image in the Canvas area. For example, if you want Meow to crawl in and out in response to your instruction Message targeted exclusively to it:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def on_message_up(self, message):\n    if message.subtype == types.TEXT:\n        text = message.content.text\n        new_text = text + ' lol~'\n        await self.send_message(new_text, channel_id=message.channel_id, sender=message.sender, recipients=message.recipients)\n\n        if len(message.recipients) == 1 and message.recipients[0] == self.meow.character_id:\n            if text == 'up':\n                c = types.CanvasItem(text='I am up!',\n                                     path='resources/avatar.png')  # the path could be a local file or an url\n                await self.send_canvas(canvas_items=c, channel_id=message.channel_id,\n                                       recipients=message.sender)\n            elif text == 'down':\n                c = types.CanvasItem(text='I am down!')\n                await self.send_canvas(canvas_items=c, channel_id=message.channel_id,\n                                       recipients=message.sender)\n            else:\n                await self.send_message(\"Sorry I don't get it!\", channel_id=message.channel_id,\n                                        sender=self.meow, recipients=message.sender)\n        else:\n            pass\n\n    else:\n        await self.send_message(message)\n"})}),"\n",(0,t.jsx)(n.p,{children:"The canvas in the Channel is by default hidden, and you have to show it by clicking the"}),"\n",(0,t.jsx)(n.p,{children:(0,t.jsx)(n.img,{alt:"moobius",src:s(5889).A+"",width:"70",height:"71"})}),"\n",(0,t.jsxs)(n.p,{children:['icon next to "Canvas" on the upper right-hand side of the webpage. Now try sending "up", "down" or anything else targeted to Meow. You will find your Channel\'s Canvas updated. Like Characters and Messages, The Canvas of different Members are independent (Meow could be on User A\'s Canvas but not on User B\'s). In the above code, the ',(0,t.jsx)(n.code,{children:"send_canvas()"})," method of our SDK sends an ",(0,t.jsx)(n.code,{children:"update_canvas"})," Downward Event, which is somehow similar as ",(0,t.jsx)(n.code,{children:"send_message()"}),", but its argument ",(0,t.jsx)(n.code,{children:"canvas_items"})," should be a ",(0,t.jsx)(n.code,{children:"CanvasItem"})," instance, or a list of that, to be sent as the data payload to the Canvas. A ",(0,t.jsx)(n.code,{children:"CanvasItem"})," consists of two optional fields: ",(0,t.jsx)(n.code,{children:"path"})," (for image) and ",(0,t.jsx)(n.code,{children:"text"})," (for text), which determines what to display on the Canvas."]}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsxs)(n.ol,{children:["\n",(0,t.jsxs)(n.li,{children:["If ",(0,t.jsx)(n.code,{children:"path"})," and ",(0,t.jsx)(n.code,{children:"text"})," are both ",(0,t.jsx)(n.code,{children:"None"})," (by default), the result will be an empty Canvas."]}),"\n",(0,t.jsxs)(n.li,{children:["You can pass the file path of a local image to ",(0,t.jsx)(n.code,{children:"path"})," argument, but the SDK will upload it to the file Service of Moobius under the hood, and use the url of the uploaded file as the actual payload sent to the Moobius Platform. Large files are not sent directly through websocket."]}),"\n",(0,t.jsxs)(n.li,{children:["If you pass a list of ",(0,t.jsx)(n.code,{children:"CanvasItem"}),' instances, they will appear in the client\'s Canvas area as one "frame" with multiple Canvas items so that a User can scroll left/right to view each of them. When a subsequent frame is sent, all of them will be overwritten.']}),"\n"]})}),"\n",(0,t.jsxs)(n.p,{children:["In the above example, the content of the Canvas won't persist if the User refreshes the browser, or switches the current Channel away and comes back, which is exactly what happened to the Character list untill the code was added to the ",(0,t.jsx)(n.code,{children:"refresh"})," Event. Hence, we may want to call ",(0,t.jsx)(n.code,{children:"send_canvas()"})," in your customized ",(0,t.jsx)(n.code,{children:"send_member_view()"}),", but before this, we have to know what exactly should be sent (with Meow or not), which means we need to store the state of each Member in each Channel, which means that we need to initialize a storage first:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def on_channel_init(self, channel_id):\n    self.channels[channel_id] = {}    # member_id -> True/False meaning up/down\n    await self.sync_channel(channel_id)\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"on_channel_init()"})," is another callback defined in ",(0,t.jsx)(n.code,{children:"Moobius"})," that is invoked almost right after ",(0,t.jsx)(n.code,{children:"before_channel_init()"}),". It accepts a ",(0,t.jsx)(n.code,{children:"channel_id"})," argument and is supposed to be called in a loop for each Channel the Service is supposed to operate on. At the time when it is invoked for the first time, the attribute ",(0,t.jsx)(n.code,{children:"self.channels"})," (automatically created by the SDK core code) contains all ChannelIDs as its keys, and the values are initially set to ",(0,t.jsx)(n.code,{children:"None"}),". In this simple demo, we assign an empty dict for each Channel, which will later be filled with ",(0,t.jsx)(n.code,{children:"member_id: True"})," or ",(0,t.jsx)(n.code,{children:"member_id: False"})," to store the Canvas state for each member (",(0,t.jsx)(n.code,{children:"True"})," means Meow up, and ",(0,t.jsx)(n.code,{children:"False"})," means Meow down). Therefore, we need to do this in ",(0,t.jsx)(n.code,{children:"sync_channel()"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def sync_channel(self, channel_id):\n    self.members[channel_id] = await self.fetch_member_ids(channel_id)\n\n    for member_id in self.members[channel_id]:\n        if member_id not in self.channels[channel_id]:\n            self.channels[channel_id][member_id] = False    # initialize\n        else:\n            pass\n\n        await self.send_member_view(channel_id, member_id)\n\n"})}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsxs)(n.ol,{children:["\n",(0,t.jsxs)(n.li,{children:["The default implementation of ",(0,t.jsx)(n.code,{children:"Moobius.on_channel_init()"})," invokes ",(0,t.jsx)(n.code,{children:"on_channel_checkin()"})," if we don't override it, which is why the Users' Views can be updated immediately after the Service restarts in our previous implementation. However, if we override it, we have to manually call ",(0,t.jsx)(n.code,{children:"sync_channel()"})," (or ",(0,t.jsx)(n.code,{children:"on_channel_checkin()"}),") to ensure the initialization."]}),"\n",(0,t.jsxs)(n.li,{children:["As you may notice, ",(0,t.jsx)(n.code,{children:"self.members"})," and ",(0,t.jsx)(n.code,{children:"self.channels"})," both have the same keys now (all ",(0,t.jsx)(n.code,{children:"channel_id"}),"s). Also ",(0,t.jsx)(n.code,{children:"self.members[channel_id]"})," and (keys of) ",(0,t.jsx)(n.code,{children:"self.channels[channel_id]"})," have the same set of ",(0,t.jsx)(n.code,{children:"member_id"}),". It seems to be better practice to do refactoring now by removing ",(0,t.jsx)(n.code,{children:"self.members"})," and substituting all occurrences of ",(0,t.jsx)(n.code,{children:"recipients=self.members[channel_id]"})," to ",(0,t.jsx)(n.code,{children:"recipients=self.channels[channel_id]"})," (these ",(0,t.jsx)(n.code,{children:"recipients"})," arguments actually accept any iterables) as indicated by Single Responsibility Principle (SRP). However, such changes may cause extra confusion for some readers and we would like to refrain from doing this now. For better practice of this sort of state management, we can use ",(0,t.jsx)(n.code,{children:"MoobiusStorage"})," covered in Advanced Tutorial."]}),"\n"]})}),"\n",(0,t.jsxs)(n.p,{children:["Next, ",(0,t.jsx)(n.code,{children:"send_member_view()"})," will also be modified to send a proper CanvasItem to a User (a ",(0,t.jsx)(n.code,{children:"send_member_canvas()"})," helper function is defined), and the Character part is moved to ",(0,t.jsx)(n.code,{children:"send_member_characters()"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def send_member_view(self, channel_id, member_id):\n    await self.send_member_characters(channel_id, member_id)\n    await self.send_member_canvas(channel_id, member_id)\n\nasync def send_member_characters(self, channel_id, member_id):\n    characters = [self.meow.character_id] + self.members[channel_id] + self.custom_agents  # Note the change\n    ind = characters.index(member_id)  # find the recipient\n    characters = [member_id] + characters[:ind] + characters[ind + 1:]  # reordered\n\n    await self.send_characters(characters=characters, channel_id=channel_id, recipients=member_id)\n\nasync def send_member_canvas(self, channel_id, member_id):\n    if self.channels[channel_id][member_id]:    # Meou Up\n        c = types.CanvasItem(text='I am up!', path='resources/avatar.png')\n        await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n    else:\n        c = types.CanvasItem(text='I am down!')\n        await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Finally, in ",(0,t.jsx)(n.code,{children:"on_message_up()"}),", we can let the state value be modified by a User's Message:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def on_message_up(self, message):\n    if message.subtype == types.TEXT:\n        text = message.content.text\n        new_text = text + ' lol~'\n        await self.send_message(new_text, channel_id=message.channel_id, sender=message.sender, recipients=message.recipients)\n\n        if len(message.recipients) == 1 and message.recipients[0] == self.meow.character_id:\n            if message.content.text == 'up':\n                self.channels[message.channel_id][message.sender] = True\n                await self.send_member_canvas(message.channel_id, message.sender)\n            elif message.content.text == 'down':\n                self.channels[message.channel_id][message.sender] = False\n                await self.send_member_canvas(message.channel_id, message.sender)\n            else:\n                await self.send_message(\"Sorry I don't get it!\", channel_id=message.channel_id, sender=self.meow,\n                                        recipients=message.sender)\n        else:\n            pass\n    else:\n        await self.send_message(message)\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The Canvas is hidden (instead of turned off) on a User's client by default, which is purely a frontend thing. Even if it is hidden, ",(0,t.jsx)(n.code,{children:"update_canvas"})," still reaches the User. If a User shows the Canvas after an ",(0,t.jsx)(n.code,{children:"update_canvas"})," Downward Event, they will still see the content. With that being said, if your Service utilizes the Canvas and you want your User to be aware of the change, you might want to send an ",(0,t.jsx)(n.code,{children:"update_style"})," Downward Event to instruct the frontend to automatically show the Canvas when your Service starts and when a User refreshes:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def on_channel_init(self, channel_id):\n    self.channels[channel_id] = {}\n    await self.sync_channel(channel_id)\n\n    s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n    await self.send_style(style_items=s, channel_id=channel_id, recipients=self.members[channel_id])\n\nasync def on_refresh(self, action):\n    await self.sync_channel(channel_id)\n\n    s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n    await self.send_style(style_items=s, channel_id=action.channel_id, recipients=self.members[action.channel_id])\n\n"})}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsxs)(n.p,{children:["the reason we don't put Style update into ",(0,t.jsx)(n.code,{children:"send_member_view()"})," is that we want to avoid it being called in the periodic ",(0,t.jsx)(n.code,{children:"on_checkin()"}),", since if a User hides the Canvas manually, we don't want it to be automatically expanded every now and then. This is a demonstration of fine-grained control of User experience. However, if you or your Users don't care about this, just send ",(0,t.jsx)(n.code,{children:"update_style"})," in ",(0,t.jsx)(n.code,{children:"send_member_view()"})," for simplicity."]})}),"\n",(0,t.jsxs)(n.p,{children:['The content on the Canvas would remain there on the screen, independent of the flow of Messages, which can be useful if you want to display something that needs to be "pinned" for a while (before the Service sends another ',(0,t.jsx)(n.code,{children:"update_canvas"})," Downward Event). However, unlike Messages, Moobius doesn't store the history of Canvas for Users, which means that a Member can only see the content from the last ",(0,t.jsx)(n.code,{children:"update_canvas"}),"; old things happening when the Member is offline or not paying attention are gone for good (well, this may not be true if the User is using a customized client from our User-mode SDK, which is covered in our Advanced Tutorial)."]}),"\n",(0,t.jsxs)(n.p,{children:["Again, even if we have ",(0,t.jsx)(n.code,{children:"self.channels"})," storing values that are related to individual Members's state of Canvas, they will be lost if you restart the Service. Extra effort is required to handle these situations, which involves storage and retrieve of each Member's state in an elegant way. As previously mentioned, ",(0,t.jsx)(n.code,{children:"MoobiusStorage"})," would be of great help in these situations, and its usage is covered in Advanced Tutorial."]}),"\n",(0,t.jsxs)(n.p,{children:["The entire code of ",(0,t.jsx)(n.code,{children:"service.py"})," up to now looks like this:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"# service.py\nfrom moobius import Moobius, MoobiusWand, types\n\n\nclass ZeroService(Moobius):\n    async def before_channel_init(self):\n        self.meow = await self.create_agent(name='Meow', avatar='resources/avatar.png')\n        self.members = {}\n        self.custom_agents = []\n\n    async def on_channel_init(self, channel_id):\n        self.channels[channel_id] = {}\n        await self.sync_channel(channel_id)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=channel_id, recipients=self.members[channel_id])\n\n    async def on_channel_checkin(self, channel_id):\n        await self.sync_channel(channel_id)\n\n    async def on_refresh(self, action):\n        await self.send_member_view(action.channel_id, action.sender)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=action.channel_id, recipients=self.members[action.channel_id])\n\n    async def on_join(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Hello! I just joined!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"Welcome!\", channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n\n    async def on_leave(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Adios! I just left!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"See you next time!\", channel_id=action.channel_id, sender=self.meow,\n                                recipients=action.sender)\n\n    async def on_message_up(self, message):\n        if message.subtype == types.TEXT:\n            text = message.content.text\n            new_text = text + ' lol~'\n            await self.send_message(new_text, channel_id=message.channel_id, sender=message.sender, recipients=message.recipients)\n\n            if len(message.recipients) == 1 and message.recipients[0] == self.meow.character_id:\n                if message.content.text == 'up':\n                    self.channels[message.channel_id][message.sender] = True\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                elif message.content.text == 'down':\n                    self.channels[message.channel_id][message.sender] = False\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                else:\n                    await self.send_message(\"Sorry I don't get it!\", channel_id=message.channel_id, sender=self.meow,\n                                            recipients=message.sender)\n            else:\n                pass\n\n        else:\n            if message.subtype in [types.IMAGE, types.AUDIO]:\n                await self.download(source=message, auto_dir='recv')\n                await self.send_message('resources/avatar.png', channel_id=message.channel_id, subtype=types.IMAGE,\n                                        sender=self.meow, recipients=message.sender)\n\n            elif message.subtype == types.FILE:\n                await self.download(source=message,\n                                    file_path=f'files/{message.content.filename}')  # dir auto created, filename specified\n                await self.send_message(f'File {message.content.filename} saved!', channel_id=message.channel_id,\n                                        sender=self.meow, recipients=message.sender)\n\n            else:\n                pass\n\n            await self.send_message(message)\n\n    async def sync_channel(self, channel_id):\n        self.members[channel_id] = await self.fetch_member_ids(channel_id)\n\n        for member_id in self.members[channel_id]:\n            if member_id not in self.channels[channel_id]:\n                self.channels[channel_id][member_id] = False  # initialize\n            else:\n                pass\n\n            await self.send_member_view(channel_id, member_id)\n\n    async def send_member_view(self, channel_id, member_id):\n        await self.send_member_characters(channel_id, member_id)\n        await self.send_member_canvas(channel_id, member_id)\n\n    async def send_member_characters(self, channel_id, member_id):\n        characters = [self.meow.character_id] + self.members[channel_id] + self.custom_agents  # Note the change\n        ind = characters.index(member_id)  # find the recipient\n        characters = [member_id] + characters[:ind] + characters[ind + 1:]  # reordered\n\n        await self.send_characters(characters=characters, channel_id=channel_id, recipients=member_id)\n\n    async def send_member_canvas(self, channel_id, member_id):\n        if self.channels[channel_id][member_id]:    # Meow Up\n            c = types.CanvasItem(text='I am up!', path='resources/avatar.png')\n            await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n        else:\n            c = types.CanvasItem(text='I am down!')\n            await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n\n\nif __name__ == \"__main__\":\n    MoobiusWand().run(ZeroService, config='config/config.json')\n\n"})}),"\n",(0,t.jsx)(n.h2,{id:"button-and-dialog",children:"Button and Dialog"}),"\n",(0,t.jsxs)(n.p,{children:['Buttons are a characteristic feature of Moobius. Buttons provide an alternative means of interaction along with Messages, and what makes a GBA more "application". The Service of a Channel could display a list of Buttons to a Member with an ',(0,t.jsx)(n.code,{children:"update_buttons"})," Downward Event (like other things, the Button list is also individualized, which means different Members can see different Buttons), and when a Member clicks a Button, a ",(0,t.jsx)(n.code,{children:"button_click"})," Upward Event is sent to the Service. You can see these things in logs, similar as in previous sections, and we will avoid being verbose on that. Our Python SDK has implemented senders and triggers for Buttons."]}),"\n",(0,t.jsxs)(n.p,{children:["Below is an example of displaying a simple Button that says Meow. You can create a Button by specifying its ",(0,t.jsx)(n.code,{children:"button_id"})," (for your own reference about which Button is clicked) and its ",(0,t.jsx)(n.code,{children:"button_text"})," (the text displayed on the Button). As you may realize, you can add the logic of sending buttons ",(0,t.jsx)(n.code,{children:"send_member_buttons()"})," to ",(0,t.jsx)(n.code,{children:"send_member_view()"})," like this:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def send_member_view(self, channel_id, member_id):\n    await self.send_member_characters(channel_id, member_id)\n    await self.send_member_canvas(channel_id, member_id)\n    await self.send_member_buttons(channel_id, member_id)\n\nasync def send_member_buttons(self, channel_id, member_id):\n    simple_button = types.Button(button_id='meow', button_text='MEOW')\n    await self.send_buttons(buttons=simple_button, channel_id=channel_id, recipients=member_id)\n"})}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsxs)(n.p,{children:["the ",(0,t.jsx)(n.code,{children:"button_id"})," field is for your own reference. It is recommended to be unique for each of your Buttons, so that you will know which Button is clicked in an easy way, but it does not have to be so."]})}),"\n",(0,t.jsxs)(n.p,{children:['Restart your Service, and you will find a Button with text "MEOW" appearing on top of your Message input area. Click the Button, and you will find a ',(0,t.jsx)(n.code,{children:"button_click"})," Action in your log. The Action contains a ",(0,t.jsx)(n.code,{children:"button_id"})," field that indicates the Button the User clicked."]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-json",children:'2024-08-21 14:24:24.432 | INFO     | moobius.network.ws_client:receive:142 -  {"type": "action", "body": {"subtype": "button_click", "button_id": "meow", "channel_id": "21cbc716-41ea-4f22-92ef-345516d92892", "arguments": [], "bottom_button_id": "confirm", "context": {}, "sender": "e91aa5bc-0001-70e8-cbf5-2fcafefa322b"}}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["This Action will trigger the ",(0,t.jsx)(n.code,{children:"on_button_click()"})," method defined in our SDK, so that the Service can handle accordingly. For instance:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-json",children:"async def on_button_click(self, action):\n    if action.button_id == 'meow':\n        await self.send_message('I clicked Meow!', channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n    else:\n        pass\n"})}),"\n",(0,t.jsxs)(n.p,{children:["With this implementation, whenever a Member clicks the Button ",(0,t.jsx)(n.code,{children:"meow"}),", a Message will be sent to all Members in the Channel on the clicker's behalf."]}),"\n",(0,t.jsxs)(n.p,{children:["The Button demonstrated above is a simple one, and a ",(0,t.jsx)(n.code,{children:"button_click"}),' Action is sent right after the Button is clicked. You can also have a "complex" version of Button that pops up a Dialog so that a User can input some arguments there, and a ',(0,t.jsx)(n.code,{children:"button_click"})," Action is sent when the User submits the arguments by clicking a Bottom Button that appears at the Bottom of the Dialog. Here is an example, and you can just copy-paste the code and restart the Service, then we will explain what happened:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def send_member_buttons(self, channel_id, member_id):\n    simple_button = types.Button(button_id='meow', button_text='MEOW')\n\n    ic1 = types.InputComponent(label='What would you like to say?', type=types.TEXT)\n    ic2 = types.InputComponent(label='How many times to repeat?', type=types.DROPDOWN, choices=['1', '2', '3'], required=True)\n    dialog = types.Dialog(title='REPEAT', components=[ic1, ic2])\n\n    dialog_button = types.Button(button_id='repeat', button_text='REPEAT', dialog=dialog)\n\n    await self.send_buttons(buttons=[simple_button, dialog_button], channel_id=channel_id, recipients=member_id)\n\nasync def on_button_click(self, action):\n    if action.button_id == 'meow':\n        await self.send_message('I clicked Meow!', channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n    elif action.button_id == 'repeat':\n        text = action.arguments[0].value\n        rep = int(action.arguments[1].value)  # The value is a str\n        text = text * rep\n\n        await self.send_message(text, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n    else:\n        pass\n"})}),"\n",(0,t.jsxs)(n.p,{children:["You can find a ",(0,t.jsx)(n.code,{children:"repeat"})," Button in your Channel, and after you click it, a Dialog will pop up. The Dialog consists of a title, a list of input components (each with a label and an input area), and one or more Bottom Buttons. Line 4-6 in the previous code snippet shows how to construct an InputComponent and how to construct a Dialog on top of that.\n",(0,t.jsx)(n.img,{alt:"moobius",src:s(1797).A+"",width:"599",height:"383"})]}),"\n",(0,t.jsxs)(n.p,{children:["Once we have the Dialog, we can construct a Button that pops up the Dialog by passing the ",(0,t.jsx)(n.code,{children:"dialog"})," argument to it (Line 8), and send an ",(0,t.jsx)(n.code,{children:"update_buttons"})," Downward Event in ",(0,t.jsx)(n.code,{children:"send_member_buttons()"}),'. Once a User clicks the "Confirm" button after filling in the form shown on the Dialog, a ',(0,t.jsx)(n.code,{children:"button_click"})," Action will be sent to the Service, which contains an ",(0,t.jsx)(n.code,{children:"argument"})," field that is a list of ",(0,t.jsx)(n.code,{children:"InputArgument"})," instances that encapsulates the input arguments from the User. The ",(0,t.jsx)(n.code,{children:"value"})," of each argument can be extracted and further handled in the ",(0,t.jsx)(n.code,{children:"on_button_click()"})," method. Note that the arguments appears in the same order as in the ",(0,t.jsx)(n.code,{children:"InputComponents"})," that defined the Button, and a ",(0,t.jsx)(n.code,{children:"value"})," is always a string even if it seems to be a number. (todo: filename?)"]}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsx)(n.p,{children:"There are a lot of GUI details about the Dialog, including the placeholder text, the input modality, and whether the field is a required one, etc. We may modify these details or introduce new interaction patterns/components in the future (for example, adding custom color to the label text) and update our SDK correspondingly. These things are not covered in this tutorial, and please refer to our API and SDK Documentation for the latest specifications."})}),"\n",(0,t.jsxs)(n.p,{children:['By default, the "Confirm" button is the only Bottom Button that appears in a Dialog. Alternatively, you can specify a customized list of Bottom Buttons. Each Bottom Button is defined by an ',(0,t.jsx)(n.code,{children:"id"})," field (recommended to be unique but not required), a ",(0,t.jsx)(n.code,{children:"text"})," field and a ",(0,t.jsx)(n.code,{children:"submit"})," field. ",(0,t.jsx)(n.code,{children:"text"})," specifies the text on the Bottom Button, and ",(0,t.jsx)(n.code,{children:"submit"})," is a boolean value that indicates whether a ",(0,t.jsx)(n.code,{children:"button_click"}),' Event is actually sent (along with the form). A typical use case is the "Confirmation Dialog" scenario (like when you try to delete some file from your own computer and the operating system asks for confirmation). When a User clicks any Bottom Button, the dialogue will always be closed, but the ',(0,t.jsx)(n.code,{children:"button_click"})," Action will not be sent unless the submit field of the clicked Bottom Button is true. Obviously, the ",(0,t.jsx)(n.code,{children:"submit"}),' field of the default "Confirm" Button is ',(0,t.jsx)(n.code,{children:"true"}),'. We can modify our "repeat" Button\'s Dialog to have multiple Bottom Buttons for extra repeats:']}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def send_member_buttons(self, channel_id, member_id):\n    simple_button = types.Button(button_id='meow', button_text='MEOW')\n\n    ic1 = types.InputComponent(label='What would you like to say?', type=types.TEXT)\n    ic2 = types.InputComponent(label='How many times to repeat?', type=types.DROPDOWN, choices=['1', '2', '3'],\n                               required=True)\n\n    bottom1 = types.BottomButton(id='cancel', text='NO!', submit=False)\n    bottom2 = types.BottomButton(id='double', text='Extra 2x', submit=True)\n    bottom3 = types.BottomButton(id='triple', text='Extra 3x', submit=True)\n\n    dialog = types.Dialog(title='REPEAT', components=[ic1, ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n\n    dialog_button = types.Button(button_id='repeat', button_text='REPEAT', dialog=dialog)\n\n    await self.send_buttons(buttons=[simple_button, dialog_button], channel_id=channel_id, recipients=member_id)\n\nasync def on_button_click(self, action):\n    if action.button_id == 'meow':\n        await self.send_message('I clicked Meow!', channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n    elif action.button_id == 'repeat':\n        text = action.arguments[0].value\n        rep = int(action.arguments[1].value)  # The value is a str\n\n        if action.bottom_button_id == 'double':\n            text = text * rep * 2\n        elif action.bottom_button_id == 'triple':\n            text = text * rep * 3\n        else:\n            pass\n\n        await self.send_message(text * rep, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n    else:\n        pass\n"})}),"\n",(0,t.jsxs)(n.p,{children:['Note that if the User clicks the Bottom Button "NO!", then no ',(0,t.jsx)(n.code,{children:"button_click"})," Action will even be sent to the Service (which is the same behavior as in the case when the User just clicks 'x' to close the Dialog). Also, the ",(0,t.jsx)(n.code,{children:"bottom_button_id"})," will be encapsulated in the ",(0,t.jsx)(n.code,{children:"action"})," argument passed into ",(0,t.jsx)(n.code,{children:"on_button_click()"})," so that we will know which Bottom Button is actually clicked, and the text for ",(0,t.jsx)(n.code,{children:"message_down"})," sent to the User can be generated accordingly."]}),"\n",(0,t.jsx)(n.p,{children:"The code up to now looks like this:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"# service.py\nfrom moobius import Moobius, MoobiusWand, types\n\n\nclass ZeroService(Moobius):\n    async def before_channel_init(self):\n        self.meow = await self.create_agent(name='Meow', avatar='resources/avatar.png')\n        self.members = {}\n        self.custom_agents = []\n\n    async def on_channel_init(self, channel_id):\n        self.channels[channel_id] = {}\n        await self.sync_channel(channel_id)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=channel_id, recipients=self.members[channel_id])\n\n    async def on_channel_checkin(self, channel_id):\n        await self.sync_channel(channel_id)\n\n    async def on_refresh(self, action):\n        await self.send_member_view(action.channel_id, action.sender)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=action.channel_id, recipients=self.members[action.channel_id])\n\n    async def on_join(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Hello! I just joined!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"Welcome!\", channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n\n    async def on_leave(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Adios! I just left!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"See you next time!\", channel_id=action.channel_id, sender=self.meow,\n                                recipients=action.sender)\n\n    async def on_button_click(self, action):\n        if action.button_id == 'meow':\n            await self.send_message('I clicked Meow!', channel_id=action.channel_id, sender=action.sender,\n                                    recipients=self.members[action.channel_id])\n        elif action.button_id == 'repeat':\n            text = action.arguments[0].value\n            rep = int(action.arguments[1].value)  # The value is a str\n\n            if action.bottom_button_id == 'double':\n                text = text * rep * 2\n            elif action.bottom_button_id == 'triple':\n                text = text * rep * 3\n            else:\n                pass\n\n            await self.send_message(text, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n        else:\n            pass\n\n    async def on_message_up(self, message):\n        if message.subtype == types.TEXT:\n            text = message.content.text\n            new_text = text + ' lol~'\n            await self.send_message(new_text, channel_id=message.channel_id, sender=message.sender, recipients=message.recipients)\n\n            if len(message.recipients) == 1 and message.recipients[0] == self.meow.character_id:\n                if message.content.text == 'up':\n                    self.channels[message.channel_id][message.sender] = True\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                elif message.content.text == 'down':\n                    self.channels[message.channel_id][message.sender] = False\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                else:\n                    await self.send_message(\"Sorry I don't get it!\", channel_id=message.channel_id, sender=self.meow,\n                                            recipients=message.sender)\n            else:\n                pass\n\n        else:\n            if message.subtype in [types.IMAGE, types.AUDIO]:\n                await self.download(source=message,\n                                    auto_dir='recv')  # the directory is automatically created and filename is generated\n                await self.send_message('resources/avatar.png', channel_id=message.channel_id, subtype=types.IMAGE,\n                                        sender=self.meow, recipients=message.sender)\n\n            elif message.subtype == types.FILE:\n                await self.download(source=message,\n                                    file_path=f'files/{message.content.filename}')  # dir auto created, filename specified\n                await self.send_message(f'File {message.content.filename} saved!', channel_id=message.channel_id,\n                                        sender=self.meow, recipients=message.sender)\n\n            else:\n                pass\n\n            await self.send_message(message)\n\n    async def sync_channel(self, channel_id):\n        self.members[channel_id] = await self.fetch_member_ids(channel_id)\n\n        for member_id in self.members[channel_id]:\n            if member_id not in self.channels[channel_id]:\n                self.channels[channel_id][member_id] = False  # initialize\n            else:\n                pass\n\n            await self.send_member_view(channel_id, member_id)\n\n    async def send_member_view(self, channel_id, member_id):\n        await self.send_member_characters(channel_id, member_id)\n        await self.send_member_canvas(channel_id, member_id)\n        await self.send_member_buttons(channel_id, member_id)\n\n    async def send_member_characters(self, channel_id, member_id):\n        characters = [self.meow.character_id] + self.members[channel_id] + self.custom_agents  # Note the change\n        ind = characters.index(member_id)  # find the recipient\n        characters = [member_id] + characters[:ind] + characters[ind + 1:]  # reordered\n\n        await self.send_characters(characters=characters, channel_id=channel_id, recipients=member_id)\n\n    async def send_member_canvas(self, channel_id, member_id):\n        if self.channels[channel_id][member_id]:    # Meow Up\n            c = types.CanvasItem(text='I am up!', path='resources/avatar.png')\n            await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n        else:\n            c = types.CanvasItem(text='I am down!')\n            await self.send_canvas(canvas_items=c, channel_id=channel_id, recipients=member_id)\n\n    async def send_member_buttons(self, channel_id, member_id):\n        simple_button = types.Button(button_id='meow', button_text='MEOW')\n\n        ic1 = types.InputComponent(label='What would you like to say?', type=types.TEXT)\n        ic2 = types.InputComponent(label='How many times to repeat?', type=types.DROPDOWN, choices=['1', '2', '3'],\n                                   required=True)\n\n        bottom1 = types.BottomButton(id='cancel', text='NO!', submit=False)\n        bottom2 = types.BottomButton(id='double', text='Extra 2x', submit=True)\n        bottom3 = types.BottomButton(id='triple', text='Extra 3x', submit=True)\n\n        dialog = types.Dialog(title='REPEAT', components=[ic1, ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n\n        dialog_button = types.Button(button_id='repeat', button_text='REPEAT', dialog=dialog)\n\n        await self.send_buttons(buttons=[simple_button, dialog_button], channel_id=channel_id, recipients=member_id)\n\n\nif __name__ == \"__main__\":\n    MoobiusWand().run(ZeroService, config='config/config.json')\n\n"})}),"\n",(0,t.jsx)(n.h2,{id:"context-menu",children:"Context Menu"}),"\n",(0,t.jsxs)(n.p,{children:["A Context Menu is a pop-up menu which appears when a User right-clicks a Message. A Context Menu typically has one or more clickable Menu Items, each item is very similar as a Button. A Menu Item may or may not pop up a new Dialog, and the Dialog triggered by a Menu Item is basically the same as those triggered by Buttons. When a Member clicks a Menu Item (and submits the arguments from Dialog, if applicable), a ",(0,t.jsx)(n.code,{children:"menu_item_click"})," Action is sent to the Service which can be seen in the log. This Action will invoke the ",(0,t.jsx)(n.code,{children:"on_menu_item_click()"})," method defined in the class ",(0,t.jsx)(n.code,{children:"Moobius"})," in our Python SDK."]}),"\n",(0,t.jsxs)(n.p,{children:["In order to allow users to use context menus, the Service must call ",(0,t.jsx)(n.code,{children:"send_menu()"})," to send an ",(0,t.jsx)(n.code,{children:"update_menu"})," Downward Event to the Member(s). The first argument can be one ",(0,t.jsx)(n.code,{children:"MenuItem"})," instance or a list of it. Each ",(0,t.jsx)(n.code,{children:"MenuItem"})," has a ",(0,t.jsx)(n.code,{children:"message_subtypes"}),' attribute that indicates which Message subtypes this Menu Item will appear for, which allows different Menu Items to be "assembled" differently for different Message subtypes. For instance, you may want an "OCR" Menu Item to appear only in image Messages, but not text or audio Messages.']}),"\n",(0,t.jsxs)(n.p,{children:["The data fields of a ",(0,t.jsx)(n.code,{children:"menu_item_click"})," Action are similar as a ",(0,t.jsx)(n.code,{children:"button_click"})," Action, but with extra fields of ",(0,t.jsx)(n.code,{children:"message_id"}),", ",(0,t.jsx)(n.code,{children:"message_subtype"})," and ",(0,t.jsx)(n.code,{children:"message_content"})," indicating the clicked Message (which can be either a ",(0,t.jsx)(n.code,{children:"message_up"})," or a ",(0,t.jsx)(n.code,{children:"message_down"}),")."]}),"\n",(0,t.jsx)(n.admonition,{type:"tip",children:(0,t.jsxs)(n.p,{children:["The Message is not entirely repeated in the ",(0,t.jsx)(n.code,{children:"menu_item_click"})," Event. Some fields like ",(0,t.jsx)(n.code,{children:"timestamp"}),", ",(0,t.jsx)(n.code,{children:"recipients"})," etc are amiss. However, the ",(0,t.jsx)(n.code,{children:"message_id"})," provides a unique reference to a ",(0,t.jsx)(n.code,{children:"previous"})," Message, which should be received by the Service. For typical usages, the Service does not have to store the entire Message history, and ",(0,t.jsx)(n.code,{children:"message_content"})," and ",(0,t.jsx)(n.code,{children:"message_subtype"})," provides enough information. However, the Service can choose to do that and query the ",(0,t.jsx)(n.code,{children:"message_id"})," if they really want every detail of the Message that is right-clicked."]})}),"\n",(0,t.jsxs)(n.p,{children:["The following code snippet demonstrates two types of Menu Items. The ",(0,t.jsx)(n.code,{children:"mi_type"}),' ("What type is it?") does not pop up a Dialog and appears for text, image and audio Messages. The ',(0,t.jsx)(n.code,{children:"mi_repeat"}),' ("Repeat!") pops up a Dialog, and only appears for text Messages. (Note no Context Menu for file Messages). Given the fact that we are using similar Dialogs for the "REPEAT" Button and ',(0,t.jsx)(n.code,{children:"mi_repeat"})," Menu Item, we can just refactor and move the initialization for these UI resources, among others, into ",(0,t.jsx)(n.code,{children:"before_channel_init()"})," and simplify ",(0,t.jsx)(n.code,{children:"send_member_buttons()"})," and ",(0,t.jsx)(n.code,{children:"send_member_canvas()"})," as well:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def before_channel_init(self):\n    self.meow = await self.create_agent(name='Meow', avatar='resources/avatar.png')\n    self.members = {}\n    self.custom_agents = []\n\n    self.meow_up_canvas = types.CanvasItem(text='I am up!', path='resources/avatar.png')\n    self.meow_down_canvas = types.CanvasItem(text='I am down!')\n\n    ic1 = types.InputComponent(label='What would you like to say?', type=types.TEXT)\n    ic2 = types.InputComponent(label='How many times to repeat?', type=types.DROPDOWN, choices=['1', '2', '3'],\n                               required=True)\n\n    bottom1 = types.BottomButton(id='cancel', text='NO!', submit=False)\n    bottom2 = types.BottomButton(id='double', text='Extra 2x', submit=True)\n    bottom3 = types.BottomButton(id='triple', text='Extra 3x', submit=True)\n\n    button_dialog = types.Dialog(title='REPEAT', components=[ic1, ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n    menu_dialog = types.Dialog(title='REPEAT THE MESSAGE', components=[ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n\n    self.simple_button = types.Button(button_id='meow', button_text='MEOW')\n    self.dialog_button = types.Button(button_id='repeat', button_text='REPEAT', dialog=button_dialog)\n\n    self.simple_menu_item = types.MenuItem(menu_item_id='mi_type', menu_item_text='What type is it?', message_subtypes=[types.TEXT, types.AUDIO, types.IMAGE])\n    self.dialog_menu_item = types.MenuItem(menu_item_id='mi_repeat', menu_item_text='Repeat!', message_subtypes=[types.TEXT], dialog=menu_dialog)\n\nasync def send_member_view(self, channel_id, member_id):\n    await self.send_member_characters(channel_id, member_id)\n    await self.send_member_canvas(channel_id, member_id)\n    await self.send_member_buttons(channel_id, member_id)\n    await self.send_member_menus(channel_id, member_id)\n\nasync def send_member_canvas(self, channel_id, member_id):\n    if self.channels[channel_id][member_id]:    # Meow Up\n        await self.send_canvas(canvas_items=self.meow_up_canvas, channel_id=channel_id, recipients=member_id)\n    else:\n        c = types.CanvasItem(text='I am down!')\n        await self.send_canvas(canvas_items=self.meow_down_canvas, channel_id=channel_id, recipients=member_id)\n\nasync def send_member_buttons(self, channel_id, member_id):\n    await self.send_buttons(buttons=[self.simple_button, self.dialog_button], channel_id=channel_id, recipients=member_id)\n\nasync def send_member_menus(self, channel_id, member_id):\n    await self.send_menu(menu_items=[self.simple_menu_item, self.dialog_menu_item], channel_id=channel_id, recipients=member_id)\n"})}),"\n",(0,t.jsx)(n.p,{children:"Now restart your Service, and send different types of Messages and right-click on each one (Both Upward and Downward), and you can find Menu Items appearing (or not appearing) as expected."}),"\n",(0,t.jsxs)(n.p,{children:["Tip: Although not demonstrated here, the Buttons and Menu can be individualized and the management of these UI components can be non-trivial in a more complex CCS app. For this simple example, we can put everything inside ",(0,t.jsx)(n.code,{children:"before_channel_init()"}),", but for complicated scenarios, ",(0,t.jsx)(n.code,{children:"MoobiusStorage"})," could be really helpful. Please see our Advanced Tutorial."]}),"\n",(0,t.jsxs)(n.p,{children:["The last step is to handle the ",(0,t.jsx)(n.code,{children:"menu_item_click"})," Action properly (which, like ",(0,t.jsx)(n.code,{children:"button_click"}),", you can see in console logs). We hope you already know the drill and can write something like this:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def on_menu_item_click(self, action):\n    if action.menu_item_id == 'mi_type':\n        reply = f'The type of the message you clicked is {action.message_subtype}'\n        await self.send_message(reply, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n    elif action.menu_item_id == 'mi_repeat':\n        text = action.message_content.text\n        rep = int(action.arguments[0].value)\n\n        if action.bottom_button_id == 'double':\n            await self.send_message(text * rep * 2, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n        elif action.bottom_button_id == 'triple':\n            await self.send_message(text * rep * 3, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n        else:\n            pass\n    else:\n        pass\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Restart your Service and test your Menu. If everything works fine, that means you have already graduated from the Basic Tutorial and you are able to develop a new Moobius GBA on your own now! Let's congratulate it by giving your Channel a new name and description with ",(0,t.jsx)(n.code,{children:"update_channel()"})," (of course you can call it anywhere you like, but ",(0,t.jsx)(n.code,{children:"sync_channel()"})," is a convenient place to put all the updates):"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"async def sync_channel(self, channel_id):\n    await self.update_channel(channel_id=channel_id, channel_name='Graduated!', channel_desc='I know Moobius now!')\n    self.members[channel_id] = await self.fetch_member_ids(channel_id)\n\n    for member_id in self.members[channel_id]:\n        if member_id not in self.channels[channel_id]:\n            self.channels[channel_id][member_id] = False  # initialize\n        else:\n            pass\n\n        await self.send_member_view(channel_id, member_id)\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The code of the entire ",(0,t.jsx)(n.code,{children:"service.py"})," is still less than 200 lines, but it is a full-fledged real-world demo of what a GBA could look like and covers most of the important features. We hope you already get some idea of how Moobius Platform and SDK works. We encourage you to share your GBA with your fellows (they will be excited about your work!). Also, you can learn our Advanced Tutorial (Coming Soon) for a deeper grasp of other amazing features of our SDK for those who want to deal with complex scenarios with Moobius as an expert. Again, let's share the final code, but rename ",(0,t.jsx)(n.code,{children:"ZeroService"})," to ",(0,t.jsx)(n.code,{children:"TutorialService"}),"."]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-python",children:"# service.py\nfrom moobius import Moobius, MoobiusWand, types\n\n\nclass TutorialService(Moobius):\n    async def before_channel_init(self):\n        self.meow = await self.create_agent(name='Meow', avatar='resources/avatar.png')\n        self.members = {}\n        self.custom_agents = []\n\n        self.meow_up_canvas = types.CanvasItem(text='I am up!', path='resources/avatar.png')\n        self.meow_down_canvas = types.CanvasItem(text='I am down!')\n\n        ic1 = types.InputComponent(label='What would you like to say?', type=types.TEXT)\n        ic2 = types.InputComponent(label='How many times to repeat?', type=types.DROPDOWN, choices=['1', '2', '3'],\n                                   required=True)\n\n        bottom1 = types.BottomButton(id='cancel', text='NO!', submit=False)\n        bottom2 = types.BottomButton(id='double', text='Extra 2x', submit=True)\n        bottom3 = types.BottomButton(id='triple', text='Extra 3x', submit=True)\n\n        button_dialog = types.Dialog(title='REPEAT', components=[ic1, ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n        menu_dialog = types.Dialog(title='REPEAT THE MESSAGE', components=[ic2], bottom_buttons=[bottom1, bottom2, bottom3])\n\n        self.simple_button = types.Button(button_id='meow', button_text='MEOW')\n        self.dialog_button = types.Button(button_id='repeat', button_text='REPEAT', dialog=button_dialog)\n\n        self.simple_menu_item = types.MenuItem(menu_item_id='mi_type', menu_item_text='What type is it?', message_subtypes=[types.TEXT, types.AUDIO, types.IMAGE])\n        self.dialog_menu_item = types.MenuItem(menu_item_id='mi_repeat', menu_item_text='Repeat!', message_subtypes=[types.TEXT], dialog=menu_dialog)\n\n    async def on_channel_init(self, channel_id):\n        self.channels[channel_id] = {}\n        await self.sync_channel(channel_id)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=channel_id, recipients=self.members[channel_id])\n\n    async def on_channel_checkin(self, channel_id):\n        await self.sync_channel(channel_id)\n\n    async def on_refresh(self, action):\n        await self.send_member_view(action.channel_id, action.sender)\n\n        s = types.StyleItem(widget=types.CANVAS, display='visible', expand=True)\n        await self.send_style(style_items=s, channel_id=action.channel_id, recipients=self.members[action.channel_id])\n\n    async def on_join(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Hello! I just joined!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"Welcome!\", channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n\n    async def on_leave(self, action):\n        await self.sync_channel(action.channel_id)\n        await self.send_message(\"Adios! I just left!\", channel_id=action.channel_id, sender=action.sender,\n                                recipients=self.members[action.channel_id])\n        await self.send_message(\"See you next time!\", channel_id=action.channel_id, sender=self.meow,\n                                recipients=action.sender)\n\n    async def on_button_click(self, action):\n        if action.button_id == 'meow':\n            await self.send_message('I clicked Meow!', channel_id=action.channel_id, sender=action.sender,\n                                    recipients=self.members[action.channel_id])\n        elif action.button_id == 'repeat':\n            text = action.arguments[0].value\n            rep = int(action.arguments[1].value)  # The value is a str\n\n            if action.bottom_button_id == 'double':\n                text = text * rep * 2\n            elif action.bottom_button_id == 'triple':\n                text = text * rep * 3\n            else:\n                pass\n\n            await self.send_message(text, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n        else:\n            pass\n\n    async def on_menu_item_click(self, action):\n        if action.menu_item_id == 'mi_type':\n            reply = f'The type of the message you clicked is {action.message_subtype}'\n            await self.send_message(reply, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n        elif action.menu_item_id == 'mi_repeat':\n            text = action.message_content.text\n            rep = int(action.arguments[0].value)\n\n            if action.bottom_button_id == 'double':\n                await self.send_message(text * rep * 2, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n            elif action.bottom_button_id == 'triple':\n                await self.send_message(text * rep * 3, channel_id=action.channel_id, sender=self.meow, recipients=action.sender)\n            else:\n                pass\n        else:\n            pass\n\n    async def on_message_up(self, message):\n        if message.subtype == types.TEXT:\n            text = message.content.text\n            new_text = text + ' lol~'\n            await self.send_message(new_text, channel_id=message.channel_id, sender=message.sender, recipients=message.recipients)\n\n            if len(message.recipients) == 1 and message.recipients[0] == self.meow.character_id:\n                if message.content.text == 'up':\n                    self.channels[message.channel_id][message.sender] = True\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                elif message.content.text == 'down':\n                    self.channels[message.channel_id][message.sender] = False\n                    await self.send_member_canvas(message.channel_id, message.sender)\n                else:\n                    await self.send_message(\"Sorry I don't get it!\", channel_id=message.channel_id, sender=self.meow,\n                                            recipients=message.sender)\n            else:\n                pass\n\n        else:\n            if message.subtype in [types.IMAGE, types.AUDIO]:\n                await self.download(source=message,\n                                    auto_dir='recv')  # the directory is automatically created and filename is generated\n                await self.send_message('resources/avatar.png', channel_id=message.channel_id, subtype=types.IMAGE,\n                                        sender=self.meow, recipients=message.sender)\n\n            elif message.subtype == types.FILE:\n                await self.download(source=message,\n                                    file_path=f'files/{message.content.filename}')  # dir auto created, filename specified\n                await self.send_message(f'File {message.content.filename} saved!', channel_id=message.channel_id,\n                                        sender=self.meow, recipients=message.sender)\n\n            else:\n                pass\n\n            await self.send_message(message)\n\n    async def sync_channel(self, channel_id):\n        await self.update_channel(channel_id=channel_id, channel_name='Graduated!', channel_desc='I know Moobius now!')\n        self.members[channel_id] = await self.fetch_member_ids(channel_id)\n\n        for member_id in self.members[channel_id]:\n            if member_id not in self.channels[channel_id]:\n                self.channels[channel_id][member_id] = False  # initialize\n            else:\n                pass\n\n            await self.send_member_view(channel_id, member_id)\n\n    async def send_member_view(self, channel_id, member_id):\n        await self.send_member_characters(channel_id, member_id)\n        await self.send_member_canvas(channel_id, member_id)\n        await self.send_member_buttons(channel_id, member_id)\n        await self.send_member_menus(channel_id, member_id)\n\n    async def send_member_characters(self, channel_id, member_id):\n        characters = [self.meow.character_id] + self.members[channel_id] + self.custom_agents  # Note the change\n        ind = characters.index(member_id)  # find the recipient\n        characters = [member_id] + characters[:ind] + characters[ind + 1:]  # reordered\n\n        await self.send_characters(characters=characters, channel_id=channel_id, recipients=member_id)\n\n    async def send_member_canvas(self, channel_id, member_id):\n        if self.channels[channel_id][member_id]:    # Meow Up\n            await self.send_canvas(canvas_items=self.meow_up_canvas, channel_id=channel_id, recipients=member_id)\n        else:\n            c = types.CanvasItem(text='I am down!')\n            await self.send_canvas(canvas_items=self.meow_down_canvas, channel_id=channel_id, recipients=member_id)\n\n    async def send_member_buttons(self, channel_id, member_id):\n        await self.send_buttons(buttons=[self.simple_button, self.dialog_button], channel_id=channel_id, recipients=member_id)\n\n    async def send_member_menus(self, channel_id, member_id):\n        await self.send_menu(menu_items=[self.simple_menu_item, self.dialog_menu_item], channel_id=channel_id, recipients=member_id)\n\n\nif __name__ == \"__main__\":\n    MoobiusWand().run(TutorialService, config='config/config.json')\n"})})]})}function m(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(r,{...e})}):r(e)}},5889:(e,n,s)=>{s.d(n,{A:()=>t});const t="data:image/png;base64,"},1797:(e,n,s)=>{s.d(n,{A:()=>t});const t=s.p+"assets/images/img-5-94d355b563f9cc74dad821b06aadf73a.jpeg"},8453:(e,n,s)=>{s.d(n,{R:()=>o,x:()=>c});var t=s(6540);const a={},i=t.createContext(a);function o(e){const n=t.useContext(i);return t.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:o(e.components),t.createElement(i.Provider,{value:n},e.children)}}}]);